You can specify custom 404 errors in PHP-Router. Like this:

```php
$router->error(function() {
  // your codes.
});
```

You don't need to specify it. Because if you don't specify it, PHP-Router will be use default `404` that it has. 